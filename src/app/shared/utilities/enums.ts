export enum LANG {
  EN = 'en',
  VN = 'vn',
}

export enum Role {
  User = 'user',
  Admin = 'admin',
}

export enum LayerType {
  Marker = 'marker',
  Polygon = 'polygon',
  Rectangle = 'rectangle',
  Circle = 'circle',
  CircleMarker = 'circlemarker',
  Line = 'polyline',
}

export enum RoutingType {
  WALK = 'walk',
  VEHICLE = 'vehicle',
}
